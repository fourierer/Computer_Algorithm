# 165.比较版本号 解题思路

## 问题分析

按 `'.'` 切分为修订号列表，逐段比较整数值。长度不同则短的按 0 补齐。

## 解法一：split + 逐段比较（推荐）

### 算法步骤

1. `split('.')` 得到两个列表
2. 用 `zip` 配合 `itertools.zip_longest`（fillvalue=0）逐段比较
3. 比较时 `int()` 自动去除前导零
4. 返回 1 / -1 / 0

### 复杂度分析

- 时间复杂度：O(n + m)
- 空间复杂度：O(n + m)
