# 387.字符串中的第一个唯一字符 解题思路

## 问题分析

两次遍历：第一次统计每个字符频次，第二次找到第一个频次为 1 的字符。

## 解法一：计数表两趟扫描（推荐）

### 算法步骤

1. 用 `Counter` 或长度 26 的数组统计频次
2. 遍历 `s`，返回第一个频次为 1 的索引
3. 无则返回 -1

### 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(1)（26 个字母固定空间）
