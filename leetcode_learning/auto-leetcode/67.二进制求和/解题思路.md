# 67.二进制求和 解题思路

## 问题分析

与 415.字符串相加 完全同构，只是进制从 10 改为 2。

关键点：
- `carry = sum // 2`，当前位 `= sum % 2`
- 最后若 `carry > 0` 补最高位的 `1`

## 解法一：双指针模拟（推荐）

### 算法步骤

1. `i, j` 指向末尾，`carry = 0`
2. 逐位相加（含进位），`carry = sum // 2`，当前位 `= sum % 2`
3. 逆序拼接返回

### 复杂度分析

- 时间复杂度：O(max(m, n))
- 空间复杂度：O(max(m, n))
