# 917.仅仅反转字母 解题思路

## 问题分析

只反转字母，非字母位置不变。经典**双指针**：左右指针向中间靠拢，都停在字母上时交换。

## 解法一：双指针（推荐）

### 算法步骤

1. `l = 0, r = n - 1`，转为列表 `a`
2. while `l < r`：
   - 若 `a[l]` 不是字母，`l++`
   - 若 `a[r]` 不是字母，`r--`
   - 否则交换 `a[l], a[r]`，`l++, r--`
3. 返回 `''.join(a)`

### 复杂度分析

- 时间复杂度：O(n)
- 空间复杂度：O(n)（列表）
